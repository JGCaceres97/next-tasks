enum Environment {
  /**
   * Localhost environment.
   */
  LOCALHOST = 'localhost',

  /**
   * Development and testing environment.
   */
  DEVELOPMENT = 'development',

  /**
   * Production and user ready environment.
   */
  PRODUCTION = 'production'
}

export default Environment;
